--- a/src/job_widget.cpp
+++ b/src/job_widget.cpp
@@ -64,1 +64,1 @@
-        R"(^Transferred:\s+([0-9.]+)(\S)? \/ (\S+) (\S+), ([0-9%-]+), (\S+ \S+), (\S+) (\S+)$)"); // Starting with rclone 1.43
+        R"(^Transferred:\s+([^/]+)\s+\/\s+([^,]+),\s+([^,]+),\s+([^,]+),\s+ETA\s+(\S+).*$)"); // Modern rclone output
@@ -65,1 +65,1 @@
-    QRegExp rxErrors(R"(^Errors:\s+(\S+)$)");
+    QRegExp rxErrors(R"(^Errors:\s+(\S+).*$)");
@@ -68,1 +68,1 @@
-        R"(^Checks:\s+(\S+) \/ (\S+), ([0-9%-]+)$)");   // Starting with
+        R"(^Checks:\s+(\S+)\s+\/\s+(\S+),\s+([0-9%-]+).*$)");   // Modern rclone output
@@ -72,1 +72,1 @@
-        R"(^Transferred:\s+(\S+) \/ (\S+), ([0-9%-]+)$)"); // Starting with
+        R"(^Transferred:\s+(\S+)\s+\/\s+(\S+),\s+([0-9%-]+).*$)"); // Modern rclone output
@@ -78,1 +78,1 @@
-        R"(\*([^:]+):\s*([^%]+)% \/[a-zA-z0-9.]+, [a-zA-z0-9.]+\/s, (\w+)$)"); // Starting with rclone 1.39
+        R"(^\s*\*\s*(.+):\s*([0-9]+)%\s+\/\s+([^,]+),\s+([^,]+),\s+(\S+)\s*$)"); // Modern rclone output
@@ -106,5 +106,4 @@
-        ui.size->setText(rxSize2.cap(1) + " " + rxSize2.cap(2) + "B" + ", " +
-                         rxSize2.cap(5));
-        ui.bandwidth->setText(rxSize2.cap(6));
-        ui.eta->setText(rxSize2.cap(8));
-        ui.totalsize->setText(rxSize2.cap(3) + " " + rxSize2.cap(4));
+        ui.size->setText(rxSize2.cap(1).trimmed() + ", " + rxSize2.cap(3).trimmed());
+        ui.bandwidth->setText(rxSize2.cap(4).trimmed());
+        ui.eta->setText(rxSize2.cap(5).trimmed());
+        ui.totalsize->setText(rxSize2.cap(2).trimmed());
@@ -197,1 +196,5 @@
-        bar->setToolTip("File name: " + name + "\nFile stats" + rxProgress2.cap(0).mid(rxProgress2.cap(0).indexOf(':')));
+        const QString speed = rxProgress2.cap(4).trimmed();
+        const QString eta = rxProgress2.cap(5).trimmed();
+        bar->setFormat("%p% - " + speed + " - ETA " + eta);
+        bar->setToolTip("File name: " + name + "\nSize: " + rxProgress2.cap(3).trimmed() +
+                        "\nBandwidth: " + speed + "\nETA: " + eta);
